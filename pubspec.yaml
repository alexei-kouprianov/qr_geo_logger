name: qr_geo_logger
description: Geo-tagged QR and barcode logger
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  mobile_scanner: ^3.5.2
  geolocator: ^10.1.0
  path_provider: ^2.1.2
  share_plus: ^7.2.1
  intl: ^0.18.1

flutter:
  uses-material-design: true
